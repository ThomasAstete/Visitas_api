---
const response = await fetch("https://visitas-empresa.onrender.com/api/visitas/");
const finalizadas = await response.json();
---

<h2>Visitas Finalizadas</h2>
<ul>
    {finalizadas.map(v => (
        <li>
            <strong>{v.nombre}</strong> — Salió: {v.hora_salida}
        </li>
    ))}
</ul>
